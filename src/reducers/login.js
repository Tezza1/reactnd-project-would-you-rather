 import { LOGIN_STATUS } from '../actions/auth'

const initialState = {
  status: false
}

function login (state = initialState, action) {
  switch(action.type){
    default:
      return state
  }
}

export default login;
